<%- include('partials/head', { meta, site }) %>
<%- include('partials/header', { path, site }) %>

<main class="section">
  <div class="shell narrow-head not-found">
    <p class="eyebrow">404</p>
    <h1>Страница не найдена</h1>
    <p class="lead">Проверьте адрес или вернитесь на главную страницу.</p>
    <a class="button button-accent" href="/">На главную</a>
  </div>
</main>

<%- include('partials/footer', { site }) %>
