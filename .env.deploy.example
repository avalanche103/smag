# Локальные настройки деплоя (скопируйте в .env.deploy, файл в .gitignore)
DEPLOY_HOST=vh134.hoster.by
DEPLOY_USER=h215910
DEPLOY_PORT=22

# Домен сайта в ISPmanager (каталог ~/www/ДОМЕН/)
DEPLOY_DOMAIN=stroydelo.by
DEPLOY_PATH=

# Пароль SSH для автоматического деплоя (npm run deploy)
DEPLOY_PASS=
